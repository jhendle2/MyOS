#ifndef MATH_H
#define MATH_H

	unsigned long _pow(unsigned int a, unsigned int b); // a^b
	unsigned int _dlen(unsigned int n); // num digits
	unsigned int _disl(unsigned int a, unsigned int b); // isolate digit at index b from a

#endif // MATH_H